# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R
sudo: required 
cache: packages
addons:
    apt:
        packages:
            - libzmq3-dev
            - libhiredis-dev
